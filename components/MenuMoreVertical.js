import * as React from "react";
import { StyleSheet, View } from "react-native";

const MenuMoreVertical = () => {
  return <View style={styles.menuMoreVertical} />;
};

const styles = StyleSheet.create({
  menuMoreVertical: {
    width: 12,
    height: 12,
    overflow: "hidden",
  },
});

export default MenuMoreVertical;
